package carte;

public class Test {
	
	for(int i= 0; i<x; i++ )
		for(int j=0 ; j<x; i++)
			for(int index =0 ; index < tabObjet.length; index++){
			obj_x = rand(taille_carte);
			obj_y = rand(taille_carte);
			if()
			tabObjet[index].placer(rand(taille_carte), rand(taille_carte), "O");
			}
}
